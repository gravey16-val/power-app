/**
 * Root application shell.
 *
 * M1 scaffold only: renders a minimal placeholder so the dev server and test
 * harness work end-to-end. The two-panel layout, sidebar, and weather grid are
 * implemented in later milestones (see ARCHITECTURE.md).
 */
function App() {
  return (
    <main className="min-h-screen flex items-center justify-center text-gray-800">
      <h1 className="text-2xl font-semibold">Weather Dashboard</h1>
    </main>
  )
}

export default App
